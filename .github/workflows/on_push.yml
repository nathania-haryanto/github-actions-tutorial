name: Test on push

on:
  push

jobs:
  test:
    name: '▶️ test actions'
    runs-on: ubuntu-latest
    steps:
      - name: 'Test workflow'
        uses: nathania-haryanto/actions/.github/workflows/test.yml@v1
